import { AccountContainer, LoginForm } from 'src/components';

const Login = () => {
  return (
    <>
      <AccountContainer title='로그인!'>
        <LoginForm />
      </AccountContainer>
    </>
  );
};

export default Login;
